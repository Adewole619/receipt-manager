services:
  receipt-manager:
    build:
      context: .
      dockerfile: Dockerfile
    image: receipt-manager:local
    environment:
      DISPLAY: ${DISPLAY:-:0}
    volumes:
      - /tmp/.X11-unix:/tmp/.X11-unix:rw
    network_mode: host
    stdin_open: true
    tty: true
